# PR-371 Preserve Assigned Page Position

GitHub PR `#224`

## Summary

Improves category edit assignment UX by preserving assigned-products pagination state across assignment mutations.

- preserves assigned-products page position after assign/remove actions
- when a remove action empties the current assigned-products page, automatically steps back one page and reloads
- fixes assigned-products pagination event wiring by passing `assignedState` into category assignment action bindings
- keeps all behavior on existing category assignment endpoints

## Validation

- `go test ./internal/interfaces/http -run TestAdminHandler_StaticJS -count=1`
- `go test ./internal/interfaces/http -count=1`
 