# PR-316

## Summary

- extend verified-email gating to authenticated storefront checkout confirmation
- reuse the existing registration email-verification link flow before order placement
- return unverified customers to a renderable checkout entry point after verification

## Scope

- storefront checkout confirmation handler
- focused storefront checkout regression coverage for verified and unverified authenticated customers

## Validation

- targeted `go test ./internal/interfaces/http -run 'TestStorefrontHandler_(CheckoutFlow_Manual_OK|CheckoutConfirm_RedirectsToEmailVerification_WhenEmailUnverified|CheckoutConfirm_SanitizesServerErrors)' -count=1`