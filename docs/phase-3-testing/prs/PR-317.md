# PR-317

## Summary

- preserve late-step authenticated checkout state across the verified-email detour
- reuse the existing email-verification continue flow to return shoppers to a resumable checkout payment page instead of dropping them back at the start
- keep the earlier authenticated checkout gate from PR-316 intact for the normal entry path

## Scope

- signed storefront checkout resume state for verified-email redirects
- storefront checkout resume rendering for verified customers returning from `/account/verify-email`
- focused end-to-end storefront coverage for confirm -> verify-email -> payment resume

## Validation

- targeted `go test ./internal/interfaces/http -run 'TestStorefrontHandler_(CheckoutAddress_RedirectsToEmailVerification_WhenEmailUnverified|CheckoutConfirm_(RedirectsToEmailVerification_WhenEmailUnverified|ResumesPaymentAfterEmailVerification)|CheckoutFlow_Manual_OK|CheckoutConfirm_SanitizesServerErrors)' -count=1`