# PR-377 Product Available Categories Paging

## Summary

Adds the next usability slice to the product edit assignment surface.

- adds client-side paging controls for the product edit available-categories dropdown
- keeps the assigned-categories paging controls on the same screen
- resets both paging states when the shared category filter changes
- preserves existing assign/remove endpoints and layout

## Validation

- `go test ./internal/interfaces/http -run TestAdminHandler_StaticJS -count=1`
- `go test ./internal/interfaces/http -count=1`
