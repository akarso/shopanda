# PR-370 Assigned Products Paging Guard

## Summary

Adds a focused regression guard for the category edit assigned-products paging controls.

- updates `TestAdminHandler_StaticJS` to assert assigned-products paging labels are present in the admin JS bundle
- locks coverage for the category edit assigned-products pagination affordances (`Previous Assigned Page` / `Next Assigned Page`)
- keeps the change limited to test coverage (no runtime behavior changes)

## Validation

- `go test ./internal/interfaces/http -run TestAdminHandler_StaticJS -count=1`
 