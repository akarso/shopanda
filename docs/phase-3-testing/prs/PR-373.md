# PR-373 Category Picker Assigned Lookup Fix

## Summary

Adds the next correctness slice for category product assignment.

- keeps assigned-products table paging on the category edit page
- builds available-product filtering from the full assigned-product set (across pages), not only the currently visible assigned page
- uses paged reads on existing category assignment endpoints (no new API surface)
- prevents already-assigned products from reappearing in the assign dropdown when they are not on the current assigned-products page

## Validation

- `go test ./internal/interfaces/http -run TestAdminHandler_StaticJS -count=1`
- `go test ./internal/interfaces/http -count=1`
