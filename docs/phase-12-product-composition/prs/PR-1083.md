# PR-1083 — RUNBOOK.md + Admin GUI Consolidation Pass

Status: planned

## Summary

- Symptom → check → fix RUNBOOK.md entries for this phase's most likely support questions.
- Final admin GUI pass folding all of this phase's sections into the existing product edit screen tabs.

## Why

Closes the phase the same way Phase 11's own PR-1047 closes its phase — operational documentation and a GUI consistency pass, not new domain work. Every prior PR in this phase either has its own admin GUI section already (via PR-1053's mechanism) or a documented reason it doesn't; this PR is where someone actually clicks through the whole product edit screen end to end and confirms nothing was left as an API-only feature with no GUI path.

## Scope

- RUNBOOK.md entries: "why can't I add this product to cart" (→ check `Purchasable`, check `RequiresLinkedParent`/eligibility), "why is this product not visible" (→ walk the four-axis default-rule checklist from PR-1055), "why isn't my ticket sellable" (→ the specific `RequiresLinkedParent` case) — same symptom → check → fix style as Phase 10's incident-response section and the "Admin jobs (retry/cancel)" entry added during Phase 11.
- Full click-through audit of the admin product edit screen across every type (simple, virtual, bundle, grouped, configurable, downloadable) confirming each type's relevant sections render correctly and irrelevant sections stay hidden (PR-1053's switch mechanism).
- Phase 12 PR index / ROADMAP.md / README.md status updates → done, matching the doc-update convention every prior phase in this codebase has followed.

## Out Of Scope

- New functionality — this is a documentation and consistency-verification PR only.

## Follow-up

- None — this is the last PR in the phase.
