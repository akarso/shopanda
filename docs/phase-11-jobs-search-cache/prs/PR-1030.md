# PR-1030 — Scheduler Admin

Status: planned

## Summary

- Expose the cron scheduler's registered tasks (name, spec, next run time) as a queryable catalog, mirroring the existing hooks/slots catalog pattern.
- `GET /api/v1/admin/schedules`, `POST /api/v1/admin/schedules/{name}/trigger` (fire now), plus a persisted per-schedule enable/disable flag.

## Why

`cron.Scheduler` today only exposes `Register`/`Start`/`Stop` — there is no way to ask it "what's registered and when does it next fire" without reading source code, and no way to fire a scheduled task on demand (useful for testing a plugin's sync job, or forcing a cache-cleanup/reindex run without waiting for its tick).

## Scope

- `internal/domain/scheduler/catalog.go` (new) — `CatalogEntry{Name, Spec string; NextRun time.Time; Enabled bool}`, `Scheduler.Catalog() []CatalogEntry` — computing `NextRun` requires the cron parser to expose a "next occurrence after t" function (`internal/infrastructure/cron`'s expression parser likely already computes this internally for tick alignment; expose it as a standalone function rather than duplicating the logic).
- `internal/domain/scheduler/overrides.go` — a small persisted table, `scheduler_overrides(name TEXT PRIMARY KEY, enabled BOOLEAN NOT NULL DEFAULT true, updated_at TIMESTAMPTZ)`. `Scheduler.run` (the internal tick-fire path) checks this before invoking a registered fn; absence of a row means enabled (default-on, matching today's behavior for anyone who never touches this feature).
- `GET /api/v1/admin/schedules` — list catalog entries with enabled state.
- `POST /api/v1/admin/schedules/{name}/trigger` — invokes the registered fn immediately, out-of-band from the normal tick (same function, so "trigger now" and "the real tick fired" are indistinguishable to the fn itself — no special-casing needed in individual task implementations).
- `POST /api/v1/admin/schedules/{name}/enable` / `.../disable` — writes to `scheduler_overrides`.
- Same `jobs.read`/`jobs.write` permissions from PR-1029 gate this (schedules and jobs are the same conceptual surface — no new permission pair needed).

## Out Of Scope

- Changing a schedule's cron spec at runtime (only enable/disable) — spec changes stay code-level, matching the existing "schedules are code-registered" model; runtime spec editing is a much larger change (arbitrary cron expressions from an admin UI need real validation and are a different risk profile from a boolean toggle) and isn't asked for.
- Any change to the scheduler process's single-instance assumption (see ROADMAP's "Out of scope" — distributed scheduler/leader election is explicitly not addressed in this phase).

## Validation (planned — run when implementing)

- `go test ./internal/domain/scheduler/... ./internal/infrastructure/cron/... -count=1` — catalog `NextRun` computation against known cron specs, override enable/disable gating actual fire.
- `go test ./internal/interfaces/http/admin/... -run Schedule -count=1`
- `go build ./...`

## Documentation updates

| Artifact | Change |
| --- | --- |
| `openapi.yaml` | `/admin/schedules`, `/admin/schedules/{name}/trigger`, `/admin/schedules/{name}/enable`, `/admin/schedules/{name}/disable` |
| `RUNBOOK.md` | Note in the existing incident-response entries where relevant (e.g. "bad migration" recovery could reference triggering a schedule manually if applicable) |
| Phase 11 PR index / ROADMAP | status → done |

## Follow-up

- PR-1031 — GUI screens.
- PR-1032 — CLI parity (`schedule:list`, `schedule:trigger`).
