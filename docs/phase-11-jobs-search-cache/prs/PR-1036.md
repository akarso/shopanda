# PR-1036 — On-Save Incremental Indexing

Status: done

## Summary

- Event bus subscribers on product/price/stock/category-assignment change events, enqueuing a small, debounced reindex job scoped to the affected product/category IDs — the search index stays fresh without a manual reindex.

## Why

This is the actual gap identified in prior research: the search index does not update automatically today. A product edit, price change, or stock update leaves the index stale until someone runs `search:reindex` by hand. Every piece needed to fix this cheaply already exists after PR-1033/1034/1035 — this PR is the wiring, following the exact same shape as `internal/application/cache/invalidation.go`'s existing subscriber.

## Scope

- `internal/application/search/invalidation.go` (new) — `IndexUpdateSubscriber.Register(bus)`:
  - `bus.OnAsync(catalog.EventProductCreated, s.HandleProductCreated)` and `bus.OnAsync(catalog.EventProductUpdated, s.HandleProductUpdated)` → enqueue `ScopeProducts{IDs: [productID]}`.
  - `bus.OnAsync(pricing.EventPriceUpserted, s.HandlePriceUpserted)` → enqueue `ScopeProducts{IDs: [productID]}`.
  - `bus.OnAsync(inventory.EventStockUpdated, s.HandleStockUpdated)` — this event didn't exist; added it (`internal/domain/inventory/events.go`) and the publish call at the admin stock-adjustment path.
  - Category-assignment change — reuses `catalog.EventProductUpdated` (no new event); wired a publish call into `category_product_assignment_admin.go`'s `Assign`/`Unassign`, which previously published nothing at all.
- **Debouncing:** a 5-second in-memory, per-product-ID debounce (`IndexUpdateSubscriber.allow`, a mutex-guarded map with opportunistic pruning) coalesces a burst of edits to the same product into one enqueue. Per-process, not distributed — see "Design decisions."
- These enqueued jobs go through the same `search.reindex` job type and `ScopeProducts` path PR-1034 already built — no new job type, no new execution path.

## Out Of Scope

- Synchronous (request-blocking) on-save indexing — always async via the queue, consistent with every other side-effect-on-event pattern in this codebase (cache invalidation, webhook dispatch).
- Debouncing across a distributed multi-instance deployment — each instance debounces independently; accepted over-triggering (a few extra small reindex jobs), not a correctness problem.
- Wiring `inventory.EventStockUpdated` from the bulk ERP stock-sync importer (`StockSyncService.UpsertBySKU`) — only the interactive admin stock-adjustment endpoint publishes it. See "Design decisions."
- `IndexCategory`-based category-document indexing — still blocked on PR-1037, unrelated to this PR's product-side `CategoryIDs` refresh.

## Design decisions made during implementation

- **Category-assignment changes reuse `catalog.EventProductUpdated`, not a new event.** `category_product_assignment_admin.go`'s `Assign`/`Unassign` published nothing before this PR. Adding a dedicated `catalog.EventCategoryAssignmentChanged` would mean every future consumer needs to remember to subscribe to *two* "this product might need reindexing" events instead of one. Reusing `EventProductUpdated` (with only `ProductID` populated — no consumer, old or new, reads `ProductUpdatedData`'s other fields; every consumer re-fetches fresh data by ID) means `IndexUpdateSubscriber.HandleProductUpdated` covers this for free, and — a genuine side benefit — `cache.InvalidationSubscriber` (already subscribed to this event) now correctly invalidates the product's cache on a category-assignment change too, which it never did before.
- **`inventory.EventStockUpdated` is published only from the interactive admin `PUT /api/v1/admin/inventory/{variantId}` path, not from the bulk ERP stock-sync importer.** `StockSyncService` (used by `wireIntegrationStockSyncer`, called from `serve`, `worker`, and CLI schedule-trigger entry points) doesn't have an `event.Bus` available at several of those call sites, and threading one through all of them was a materially larger, more invasive change than this PR's actual scope justified. A bulk stock import via ERP sync doesn't trigger on-save indexing as a result — an accepted, documented gap (manual/scheduled `search:reindex` remains the fallback), not an oversight.
- **The two pre-existing inline `bus.OnAsync(catalog.EventProduct*, ...)` handlers in `wire_services.go` were removed, not kept alongside the new subscriber.** They called `searchEngine.IndexProduct` directly and synchronously (relative to the event) on every product create/update, bypassing the job queue and `search_index_runs` entirely — no retry, no debounce, no progress tracking. Keeping both would double-index every product edit (once immediately via the old path, once again a few seconds later via the new debounced job) and add a one-row `search_index_runs` entry for every ordinary product edit. Removed in favor of `IndexUpdateSubscriber` handling `EventProductCreated`/`EventProductUpdated` too — this is a deliberate architectural replacement this PR's own "Out Of Scope" section already implied ("synchronous... on-save indexing" being exactly what's being replaced), not an unplanned scope expansion. The trade-off: a product edit now takes up to ~5s (the debounce window) to reflect in search instead of near-immediately, in exchange for queue durability/retry and one consistent code path.
- **`catalog.EventProductCreated` is subscribed even though the plan's own Scope section only names "product/price/stock/category-assignment change events."** Necessary precisely because of the previous point — removing the old inline handlers without also covering `EventProductCreated` in the new subscriber would silently stop indexing brand-new products until the next scheduled full reindex, a real regression the plan didn't intend.
- **The debounce map is bounded via opportunistic pruning on every `allow` call, not a periodic sweep goroutine.** A background goroutine would need its own shutdown/leak handling this subscriber (unlike, say, the job worker) has no natural lifecycle hook for. Pruning stale entries inline — every call scans and evicts anything older than the debounce window before checking/recording the current key — keeps `lastFire` bounded to "product IDs edited within roughly the last debounce window," with no goroutine to manage.
- **`IndexUpdateSubscriber` and `ReindexService` both add `bus`/event wiring to existing HTTP admin handlers via an optional `SetBus(bus *event.Bus)` setter, not a new required constructor parameter.** `InventoryAdminHandler`, `ProductPriceAdminHandler`, and `CategoryProductAssignmentAdminHandler` each have several existing constructors used across real wiring and many tests; adding a required `bus` parameter to all of them would touch every one of those call sites for a dependency most tests don't care about. `SetBus` (left unset, `nil`, publishing is skipped rather than erroring) mirrors `Auditor.SetAuditLogRepository`'s own established optional-wiring pattern in this codebase.

## What changed

- `internal/domain/inventory/events.go` (new) — `EventStockUpdated`, `StockUpdatedData`.
- `internal/application/search/invalidation.go` (new, updated in Round 1) — `IndexUpdateSubscriber`, its four `Handle*` methods, and the debounce (`allow`/`lastFire`/`evict`).
- `internal/application/search/invalidation_test.go`, `invalidation_internal_test.go` (new) — 15 tests (after Round 1): each event type triggers a reindex, wrong event-data type errors without enqueueing, an empty product ID no-ops, a burst on one product coalesces to one enqueue, distinct products don't coalesce, a failed `Trigger` doesn't poison the debounce window, `Register(nil)`/`NewIndexUpdateSubscriber(nil, ...)` panic, and (whitebox, package `search`) the debounce window's exact boundary behavior and stale-entry pruning, using fabricated timestamps instead of real sleeps.
- `internal/interfaces/http/admin/inventory_admin.go` — `SetBus`; `Adjust` publishes `inventory.EventStockUpdated` after a successful `SetStock`.
- `internal/interfaces/http/admin/product_price_admin.go` — `SetBus`; `Update` publishes `pricing.EventPriceUpserted` after a successful `Upsert` (previously only the bulk price importer published this event — an interactive admin price edit never reached the search index, or anything else listening for this event, at all).
- `internal/interfaces/http/admin/category_product_assignment_admin.go` — `SetBus`, `publishAssignmentChanged`; `Assign`/`Unassign` now publish `catalog.EventProductUpdated` (previously published nothing); `publishAssignmentChanged` falls back to `event.Bus.PublishAsync` (Round 4) when `Publish` itself fails.
- `internal/platform/event/bus.go` (Round 4) — new `PublishAsync(evt)`, dispatching straight to async handlers, skipping sync handlers/gating entirely; `Publish`'s own async-dispatch phase extracted into a shared `dispatchAsync` so both call the same code path. `internal/platform/event/bus_test.go` — two new tests (`TestBus_PublishAsync_SkipsSyncRunsAsync`, `TestBus_PublishAsync_AfterFailedPublish_StillRunsAsyncHandlers`); every pre-existing bus test still passes unchanged, confirming `Publish`'s own behavior wasn't altered by the extraction.
- `internal/interfaces/http/admin/inventory_admin_test.go`, `product_scoped_admin_test.go`, `category_admin_test.go` — one new test per handler asserting the correct event/payload is published on success (plus one "no bus, no event, still works" test for the inventory handler).
- `cmd/api/wire_services.go` — removed the two inline `bus.OnAsync(catalog.EventProduct*, ...)` synchronous search-index-sync handlers; added `indexUpdateSubscriber := searchApp.NewIndexUpdateSubscriber(reindexService, log); indexUpdateSubscriber.Register(bus)` in their place; added `.SetBus(bus)` calls after constructing `productPriceAdmin`, `categoryProductAssignmentAdmin`, and `inventoryAdmin`.
- `RUNBOOK.md` — "Search reindex" section: new bullet describing automatic on-save indexing (event-driven, debounced, queued) and its fallback; also fixed two lines that had gone stale describing PR-1035's admin API as still-future work now that it's shipped.
- `docs/guides/DEVELOPER.md` — new "Search indexing depends on these events" subsection under "Add Custom Event Listeners," for plugin authors whose code mutates catalog/pricing/inventory data outside the normal admin save paths.
- Phase 11 PR index / ROADMAP — status → done; PR index quick-reference table split (`1036 done`, `1037–1038 planned`); new "Design notes: PR-1036's category-assignment event and the removed inline sync handlers" section.

No migration — no new tables/columns; this PR is entirely event-bus wiring plus one new domain event.

## Validation

- `go build ./...`, `go vet ./...` — clean.
- `gofmt -l` on every new/changed file — clean.
- `go test ./... -count=1` — all packages pass, no regressions (including confirming removal of the old inline `wire_services.go` handlers broke nothing — they had no dedicated test coverage of their own).
- `go test ./internal/application/search/... -run IndexUpdateSubscriber -v -count=1` — 16 tests, all passing (14 original + 1 from Round 1 + 1 from Round 2).
- `go test ./internal/interfaces/http/admin/... -run "TestInventoryAdminHandler|TestProductPriceAdmin|TestCategoryProductAssignmentAdminHandler" -v -count=1` — all passing, including the four event-emission tests (Assign/Unassign × two rounds) and the Round 3/4 publish-failure test.
- `go test ./internal/platform/event/... -v -count=1` — all passing, including the two new `PublishAsync` tests from Round 4.
- `go test ./internal/application/search/... ./internal/interfaces/http/admin/... ./internal/domain/inventory/... ./internal/platform/event/... ./cmd/api/... -race -count=1`, `-race -count=5 -run TestIndexUpdateSubscriber`, and `-race -count=5` on `internal/platform/event` — clean.
- **Not included: a full HTTP-admin-API → worker → search-engine live integration test** (the plan's own suggested validation). Each link in that chain already has its own coverage: `IndexUpdateSubscriber`'s event-to-`Trigger`-call logic is unit-tested here; `ReindexService.Trigger` → queue → `ReindexHandler` → `SearchEngine.IndexProduct` is the same job-execution path PR-1033/1034 already integration-tested; and the three HTTP publish sites are tested here to confirm they emit the right event/payload. Standing up a real worker + queue + Postgres + search engine together to re-exercise the same already-covered links end-to-end would be a substantially larger effort for confidence this test plan doesn't otherwise add. Flagged here rather than silently skipped.

## Documentation updates

| Artifact | Change |
| --- | --- |
| `RUNBOOK.md` | New bullet under "Search reindex" describing automatic on-save indexing and its fallback; two stale PR-1035-as-future-work lines corrected. |
| `docs/guides/DEVELOPER.md` | New "Search indexing depends on these events" note under "Add Custom Event Listeners." |
| Phase 11 PR index / ROADMAP | status → done; quick-reference table split; new "Design notes" section on the category-assignment event reuse and the removed inline handlers. |

## Round 1 (code review)

- **Blocker — fixed.** `scheduleReindex` called `allow(productID, now)` — which reserves the debounce slot — *before* calling `reindex.Trigger`. If `Trigger` then failed (queue full, a transient DB error), the reservation was never released: every subsequent event for that product within the remaining debounce window was silently swallowed (`scheduleReindex` returns `nil` for a debounced call), even though no reindex had actually succeeded. The index would stay stale with no recovery path except an unrelated future edit to the same product or the next scheduled full reindex. Fixed by adding `evict(productID)`, called when `Trigger` fails, releasing the reservation so the very next event for that product is genuinely retried rather than falsely debounced. New test: `TestIndexUpdateSubscriber_TriggerFailure_EvictsDebounceEntry` — uses a `fakeQueue` with `enqueueErr` set, asserts a second same-product event still reaches `Trigger` (not silently dropped) even though it fails again for the same reason; confirmed to fail against the pre-fix code (second call returns `nil`, `queue.enqueued` stays at 1) and pass against the fix (`queue.enqueued` reaches 2).
- **Major — reviewed, not currently reachable, hardened defensively.** The concern: `pricing.EventPriceUpserted` is published once per row by the bulk price importer (`internal/application/importer/prices.go`), and since this PR now subscribes to that event, a large import could fire one `Trigger` call (one `search_index_runs` row, one queued job) per distinct product — thousands for a large CSV. Checked the actual wiring: `cmd/api/io_commands.go`'s `runImportPrices` (the only production construction of `PriceImporter`) passes `bus=nil` — `imp.bus != nil` is false, so this importer **does not currently publish the event at all**, and `IndexUpdateSubscriber` is never invoked from this path today. The concern is real *if* this importer is ever wired to a real bus, but not reachable in the shipped code as of this PR. Rather than leave that as an implicit, undocumented assumption someone could break by "helpfully" passing a real bus later, added a comment at the `nil` argument explaining why it must stay `nil` absent a batching fix for the search-index side.
- **Major — reviewed, scope confirmed and expanded, documented rather than fixed.** `inventory.EventStockUpdated` is published only from the admin single-variant "Adjust" endpoint; this PR's own "Out Of Scope" only named the ERP bulk-sync importer as a known gap. Investigation surfaced this list is longer, and the highest-frequency one wasn't called out at all:
  - **Checkout reservation/release** (`internal/application/checkout.ReserveInventoryStep`) — decrements/restores `stock.quantity` via raw SQL inside `postgres.ReservationRepo.Reserve`/`Release`, never through `StockRepository`, and has no `event.Bus` wired at all. This is the single highest-frequency stock-changing path in the system (every customer purchase) and it does not keep the index fresh today.
  - **Reservation-expiry release** (`ReservationExpiryHandler`/`ReleaseExpiredBefore`) — the interface only returns a count of released reservations, not which variants changed; publishing an event here requires widening that return type across the interface, the Postgres implementation, and its test doubles first, not just adding a bus.
  - **Returns restocking** (`returns.Service.Receive`) — already has a bus (required, not optional), but has no way to resolve a return item's `VariantID` to a `ProductID` yet (`domainReturns.Item` doesn't carry one, and the service has no `catalog.VariantRepository`).
  - **Bulk stock imports** — both `import:stock` (CLI/CSV, via `importer.NewStockImporter`, which has no event-publishing capability at all) and the ERP `StockSyncService.UpsertBySKU` (already documented as a gap before this round) process many variants per call; wiring either naively would reproduce the same one-event-per-row fan-out risk as the price-importer concern above.

  None of these are small, single-line additions like the three paths this PR already wired (each needed only a `SetBus` call and a nearby dependency already in scope) — the smallest (returns) needs a new constructor dependency across one production and two test call sites; the largest (reservation-expiry) needs a public interface signature change across three layers. Fixing all of this correctly, including the fan-out risk for the two bulk paths, is a meaningfully sized follow-up PR of its own, not a review-response patch. Documented explicitly and specifically (not just "there might be gaps") in `RUNBOOK.md` and below in "Follow-up", with checkout called out as the priority given it's the highest-frequency path.
- **Minor — fixed as part of the Blocker fix above.** New test `TestIndexUpdateSubscriber_TriggerFailure_EvictsDebounceEntry` pins exactly this — the review's ask.
- **Minor — reviewed, not changed.** Async `event.Bus` handler errors are logged only, never retried at the bus level — a pre-existing characteristic of the bus itself (see `internal/platform/event/bus.go`'s own doc comment: "async handler errors are logged and do not propagate"), not something `IndexUpdateSubscriber` introduced or can change unilaterally; changing it would be a bus-wide architectural change well outside this PR's scope. The Blocker fix above removes the specific compounding failure mode the review was actually worried about (a transient `Trigger` failure permanently — for the rest of the debounce window — masking itself as "handled"); a bare log-and-drop for a genuinely one-off transient failure, with the next real save retrying it, is the same accepted trade-off the rest of this codebase's async event handlers already make.

## Round 2 (code review)

- **Blocker — fixed.** `category_product_assignment_admin.go`'s `Assign`/`Unassign` published `catalog.EventProductUpdated{ProductID: productID}` with every other field left zero-valued — including `Slug`. `rewrite.Subscriber` (`internal/application/rewrite/subscriber.go`) also subscribes to this exact event, **synchronously** (`bus.On`, not `OnAsync`), and builds a URL rewrite straight from `data.Slug` with no fallback lookup (unlike the cache-invalidation and search-index subscribers, which only ever read `ProductID` and re-fetch everything else). A blank `Slug` meant every category assignment/unassignment registered `"/"` itself as a rewrite for that product — either silently hijacking the storefront's home route to the product page, or, if `"/"` was already claimed by something else, erroring out of `saveRewrite` and aborting the whole `Publish` call before the async cache-invalidation and search-reindex handlers ever ran (a sync handler error short-circuits dispatch to async handlers — see `event.Bus`'s own doc comment). This also retroactively invalidates a claim this PR's own "Design decisions" made in Round 1's docs ("no consumer reads anything but ProductID") — corrected there too. Fixed by having `validateAssignmentTargets` return the already-fetched `*catalog.Product` (it already looked one up for validation, previously discarded) instead of a bare ID, and `publishAssignmentChanged` now builds the event from real `Name`/`Slug`/`Status`, matching how `product_admin.go` itself constructs the same event on every other product edit. New tests: `TestCategoryProductAssignmentAdminHandler_Assign_EmitsProductUpdatedEvent` extended to assert `Name`/`Slug`/`Status` (not just `ProductID`), plus a new `..._Unassign_EmitsProductUpdatedEvent` counterpart (also the review's own nitpick ask) — both confirmed to fail against the pre-fix payload (empty `Slug`) and pass against the fix.
- **Major — fixed.** The debounce was leading-edge only: `allow` fired immediately on the first event for a product, then silently swallowed every subsequent same-product event until the window elapsed, with nothing to ever pick up the swallowed event's effect if no *further* event happened to arrive after the window. In production a single-product reindex job typically completes in well under the 5-second window meant to coalesce a burst of edits — so a second real edit landing during that window had no future trigger left to reflect it, and the index would keep showing the *first* edit's state indefinitely (until an unrelated later edit to the same product, or the next scheduled full reindex) — exactly the scenario the review described (a job reading the product before a second edit commits). Fixed by adding a trailing flush: when an event is debounced, `scheduleTrailingFlush` schedules exactly one deferred `Trigger` call (`time.AfterFunc`, coalesced so a burst of debounced events only ever schedules one) timed to fire when the current window ends, guaranteeing the product's actual latest state — read live at that point, not a stale snapshot — gets reindexed even with no further event ever arriving. `IndexUpdateSubscriber` gained a `window time.Duration` field (defaulting to `productReindexDebounceWindow`, only ever overridden by a whitebox test) so the new behavior could be tested with a shrunk window instead of a multi-second real sleep. New test: `TestIndexUpdateSubscriber_TrailingFlush_CatchesLaterMutationAfterEarlyCompletion` — fires a leading-edge event, a second (debounced) event, then waits past the (shrunk) window and asserts a second `Trigger` call actually happened; confirmed to fail against the pre-fix code (only 1 call, ever) and pass against the fix (2 calls: leading + trailing).

## Round 3 (code review)

- **Major — fixed.** Even after Round 2's fix (an accurate `Name`/`Slug`/`Status` payload), `category_product_assignment_admin.go`'s `publishAssignmentChanged` still discarded `Publish`'s return value (`_ = h.bus.Publish(...)`). `rewrite.Subscriber`'s synchronous handler on `catalog.EventProductUpdated` can genuinely fail independent of payload accuracy — a real path conflict with an existing rewrite, or a repository error — and per `event.Bus`'s own contract, that aborts the whole `Publish` call before the async cache-invalidation/search-reindex handlers ever run. Silently discarding that error meant the endpoint reported success while the product's indexed category membership (and cache) went stale with no trace anywhere that anything had gone wrong. Fixed by having `publishAssignmentChanged` return the error instead of swallowing it, and having `Assign`/`Unassign` record a *second*, distinct audit entry (`Result: "error"`, a clear message) when it's non-nil — without changing the HTTP response, which still reports success: the assignment write itself already committed by that point, and this codebase's established convention (`product_admin.go`'s own `Publish` call, among others) is that a side effect's own failure never flips an admin response from success to failure. This makes a stale index/cache from this cause visible to whoever reviews the audit log, instead of invisible. New test: `TestCategoryProductAssignmentAdminHandler_Assign_PublishFailure_StillSucceedsButAudited` — registers a synchronous handler on the bus that always errors (simulating a real `rewrite.Subscriber` failure, independent of the Round 2 payload fix), asserts the response still reports `assigned: true` but exactly two audit records exist, the second being the publish-failure one; confirmed to fail against the pre-fix code (only one audit record — the failure was invisible) and pass against the fix.

## Round 4 (code review)

- **Major — fixed.** Round 3's fix made a `Publish` failure *visible* (a second audit entry) but didn't make the underlying problem go away: the endpoint still returned success while the async cache-invalidation/search-reindex handlers never ran, because `event.Bus.Publish`'s own contract aborts async dispatch entirely when a sync handler (`rewrite.Subscriber`) fails — an audit trail doesn't un-stale an index. Fixed at the right layer: added `event.Bus.PublishAsync(evt)` (`internal/platform/event/bus.go`), extracting `Publish`'s existing async-dispatch phase into a shared `dispatchAsync` so `PublishAsync` can invoke it directly — skipping sync handlers and their gating entirely, never returning an error, matching `OnAsync`'s own established contract that async handler failures are logged, not propagated. `publishAssignmentChanged` now calls `PublishAsync` as a fallback whenever `Publish` itself returns an error, so the cache-invalidation/search-reindex handlers still run even though the sync rewrite handler didn't succeed — the audit entry from Round 3 now correctly describes "a sync handler failed" rather than implying the whole update is stale, since it isn't anymore. New tests: `TestBus_PublishAsync_SkipsSyncRunsAsync` and `TestBus_PublishAsync_AfterFailedPublish_StillRunsAsyncHandlers` in `internal/platform/event/bus_test.go` (the existing `TestBus_SyncError_SkipsAsync` continues to pass unchanged, confirming `Publish`'s own behavior wasn't altered by extracting `dispatchAsync`); `TestCategoryProductAssignmentAdminHandler_Assign_PublishFailure_StillSucceedsButAudited` extended with an `OnAsync` handler standing in for cache invalidation/search reindex, asserting it still fires when the sync handler fails — confirmed to fail against the pre-fix code (async handler never ran, 2s timeout) and pass against the fix.
- **Note — deliberately not extended to `product_admin.go`.** `product_admin.go`'s own `Update()` handler publishes the exact same `catalog.EventProductUpdated` event, through the exact same discard-the-error pattern (`_ = h.bus.Publish(...)`), for every *ordinary* product field edit — not just category assignment — meaning it has the identical latent characteristic this round just fixed here: a `rewrite.Subscriber` failure on a normal product save also silently skips cache invalidation and search reindex today. This round's review comment was specifically anchored to `category_product_assignment_admin.go`; extending the same `PublishAsync`-fallback fix to `product_admin.go` (and auditing it there too) is a one-line, low-risk change using the exact primitive this round already built, but touches a different file with its own tests, outside this specific finding's scope. Flagged here rather than fixed silently or left undocumented — worth doing as an immediate, minimal follow-up given how directly analogous it is, but a deliberate choice to keep this round's diff scoped to what was actually reviewed.

## Follow-up

- PR-1037 — category-entity indexing itself (this PR only refreshes a product's own `CategoryIDs` via reindex, not a separate category document).
- **PR-1049** — closes every stock-event gap flagged in Round 1 above: checkout reservation/release (priority), reservation-expiry release, returns restocking, and a batch-once trigger strategy for the two bulk stock-mutation paths.
- **`product_admin.go`'s `Update()`** — apply the same `PublishAsync` fallback Round 4 added here, for the identical reason: a `rewrite.Subscriber` failure on an ordinary product edit currently skips cache invalidation/search reindex silently, exactly as this file's did before Round 4.
