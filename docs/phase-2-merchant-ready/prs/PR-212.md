# PR-212 — Free shipping threshold

## Goal

Add per-zone free shipping threshold so that when cart subtotal meets or
exceeds the configured amount, a zero-cost "free_shipping" rate appears
alongside normal paid rates. Implements SHIPPING_ZONES.md §4.

## Scope

| Area | Change |
|---|---|
| `internal/domain/shipping/shipment.go` | Added `MethodFreeShipping ShippingMethod = "free_shipping"`, updated `IsValid()` |
| `internal/domain/shipping/zone.go` | Added `FreeShippingThreshold shared.Money` field to `Zone` struct (zero value = disabled) |
| `internal/domain/shipping/zone_rate.go` | Added `Subtotal shared.Money` to `RateRequest`; changed `CalculateRate` return type from `(ShippingRate, error)` to `([]ShippingRate, error)`; added `qualifiesForFreeShipping` helper |
| `migrations/038_add_free_shipping_threshold.sql` | `ALTER TABLE shipping_zones ADD COLUMN free_shipping_threshold BIGINT …, ADD COLUMN free_shipping_currency TEXT …` |
| `internal/infrastructure/postgres/zone_repo.go` | Updated all zone SELECT/INSERT/UPDATE queries and scan to include `free_shipping_threshold` + `free_shipping_currency` columns |
| `internal/interfaces/http/shipping_zone_admin.go` | Added `free_shipping_threshold`/`free_shipping_currency` to create/update request structs, response struct, and `toAdminZoneResponse` |
| `internal/domain/shipping/zone_rate_test.go` | 6 new free shipping tests (20 total) |
| `internal/interfaces/http/shipping_zone_admin_test.go` | 5 new threshold admin tests (39 total) |

## Design notes

1. **FreeShippingThreshold on Zone** — `shared.Money` value. Zero amount
   means disabled. Stored as two DB columns: `free_shipping_threshold`
   (BIGINT, CHECK >= 0) and `free_shipping_currency` (TEXT).

2. **CalculateRate returns []ShippingRate** — Breaking change from single
   return. When threshold is met, slice contains paid rate + free rate.
   When not met, single-element slice with paid rate only.

3. **qualifiesForFreeShipping** — Checks three conditions: threshold
   amount > 0, threshold currency matches request currency (EqualFold),
   subtotal amount >= threshold amount.

4. **Free rate label** — Format: `"Free shipping (orders over {amount} {currency})"`.

5. **Admin CRUD** — `free_shipping_threshold` is optional on create
   (nil = 0). On update, setting to 0 clears threshold. Currency
   defaults to "EUR" when not specified.

6. **MethodFreeShipping** — New shipping method constant added to
   `IsValid()` alongside `flat_rate` and `weight_based`.

## Tests

### Domain — zone_rate_test.go (6 new, 20 total)

- `TestZoneRateCalculator_FreeShipping_ThresholdMet` — subtotal above threshold returns 2 rates
- `TestZoneRateCalculator_FreeShipping_ExactThreshold` — exact threshold qualifies
- `TestZoneRateCalculator_FreeShipping_BelowThreshold` — below threshold returns 1 rate
- `TestZoneRateCalculator_FreeShipping_Disabled` — zero threshold never produces free rate
- `TestZoneRateCalculator_FreeShipping_CurrencyMismatch` — EUR threshold + USD request = no free rate
- `TestZoneRateCalculator_FreeShipping_LabelFormat` — verifies label string format

### Handler — shipping_zone_admin_test.go (8 new, 42 total)

- `TestShippingZoneAdmin_CreateZone_WithThreshold` — create with threshold + currency
- `TestShippingZoneAdmin_CreateZone_ThresholdDefaultCurrency` — omitted currency defaults to EUR
- `TestShippingZoneAdmin_CreateZone_NoThreshold` — no threshold field = 0
- `TestShippingZoneAdmin_UpdateZone_SetThreshold` — patch threshold onto existing zone
- `TestShippingZoneAdmin_UpdateZone_ClearThreshold` — set to 0 clears threshold
- `TestShippingZoneAdmin_UpdateZone_SetThresholdDefaultCurrency` — omitted currency defaults to EUR
- `TestShippingZoneAdmin_CreateZone_NegativeThreshold` — negative rejected with 422
- `TestShippingZoneAdmin_UpdateZone_NegativeThreshold` — negative rejected with 422

## Review round

| Finding | Fix |
|---|---|
| PR doc lists "7 new" domain tests but only 6 enumerated | Corrected to "6 new, 20 total" |
| `qualifiesForFreeShipping` doesn't verify `subtotal.Currency()` matches request currency | Added `strings.EqualFold(subtotal.Currency(), currency)` check |
| CreateZone silently swallows negative threshold via `> 0` guard; inconsistent with UpdateZone | Changed to `!= 0` guard + explicit `m.Amount() < 0` rejection; same pattern in UpdateZone |
| Migration allows `free_shipping_threshold > 0` with empty currency | Added `CHECK (free_shipping_threshold = 0 OR free_shipping_currency <> '')` constraint |
| EUR-default + NewMoney logic duplicated 3 times | Extracted `buildFreeShippingMoney(amount, currency)` helper |
| Missing handler tests: update-threshold-default-currency, negative create, negative update | Added 3 new tests (42 handler total) |

## Verification

```text
go build ./...   ✅
go test ./...    ✅ (74 tests, 0 failures)
```
