# PR-215 — S3 Storage Adapter

## Scope

| Layer          | File                                              | Change                                                       |
| -------------- | ------------------------------------------------- | ------------------------------------------------------------ |
| Infrastructure | `internal/infrastructure/s3store/storage.go`      | S3 storage adapter implementing `media.Storage` interface    |
| Config         | `internal/platform/config/config.go`              | `S3StorageConfig` struct, env overlays, flatten entries       |
| Wiring         | `cmd/api/main.go`                                 | `"s3"` case in storage switch, `s3store.New()` wiring        |
| Config example | `configs/config.example.yaml`                     | S3 configuration section with documentation                  |
| Dependency     | `go.mod` / `go.sum`                               | Add `github.com/aws/aws-sdk-go-v2` + S3 service             |
| Tests          | `internal/infrastructure/s3store/storage_test.go` | 15 unit tests with mock S3 client                            |

## Design Notes

1. **Interface compliance**: `s3store.Storage` implements `media.Storage` (Name/Save/Delete/URL) — drop-in replacement for `localfs.Storage`. Compile-time check via `var _ media.Storage = (*Storage)(nil)`.

2. **S3-compatible**: Works with AWS S3, MinIO, DigitalOcean Spaces, Backblaze B2, and any S3-compatible backend. Uses `UsePathStyle = true` when a custom endpoint is set (required for MinIO).

3. **Testable via interface**: Internal `s3API` interface extracts `PutObject` and `DeleteObject` from the real S3 client. Tests use a mock implementing this interface — no network calls, no real S3 bucket needed.

4. **Upload details**: Save sets `Content-Type` (guessed from extension), `Cache-Control: public, max-age=31536000`, and `ACL: public-read`. Objects are immediately accessible via the public URL.

5. **URL resolution**: When `base_url` is configured (CDN), URLs use that prefix. Otherwise, falls back to path-style `{endpoint}/{bucket}/{key}` or AWS default `https://s3.{region}.amazonaws.com/{bucket}/{key}`.

6. **Security**: `cleanPath()` strips `..` segments, leading slashes, and empty segments from S3 keys to prevent path traversal. Access keys are loaded from env vars or YAML config.

7. **Config**: Full env overlay support: `SHOPANDA_MEDIA_S3_ENDPOINT`, `_BUCKET`, `_REGION`, `_ACCESS_KEY`, `_SECRET_KEY`, `_BASE_URL`. Flatten entries for `Get()`/`GetInt()` access.

8. **Backward-compatible**: Default storage remains `"local"`. S3 is opt-in via `media.storage: s3`.

## Tests

| File                                 | New | Total | Description                                   |
| ------------------------------------ | --- | ----- | --------------------------------------------- |
| `s3store/storage_test.go`            | 15  | 15    | Save/Delete/URL, errors, MIME types, path cleaning, New() validation |

Full suite: **0 failures** (all packages pass including new `s3store`).

## Verification

```bash
go build ./...                                           # clean
go test ./internal/infrastructure/s3store/... -v         # 15 pass
go test ./... -count=1                                   # full suite, 0 failures
```
