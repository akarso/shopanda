# PR-865 — Plugin depends_on Init Ordering

Status: done

## Summary

- Config-driven topological sort of plugin init order via `plugins.depends_on`
- Cycle detection and unknown dependency errors
- `InitAll` applies sort before calling plugin `Init`

## Config

```yaml
plugins:
  depends_on:
    exportdemo/reference:
      - importdemo/reference
```

Keys are plugin `Name()` values. Init order only — runtime hook/pipeline order unchanged.

## Validation

- `go test ./internal/platform/plugin/... -run DependsOn`
