# PR-859 — Shipping Rate Provider Port

Status: done

## Summary

- Add `shipping.ProviderRegistry` (multi-provider by method, mirrors payment)
- `RegisterShippingRateProvider` on `plugin.App`
- `resolveShippingRegistry` in `cmd/api/providers.go` with flat-rate core default
- Refactor `SelectShippingStep` to resolve provider by checkout input method
- Port introspection updated (`shipping_rate` shipped)

## Validation

- `go test ./internal/domain/shipping/... ./internal/application/checkout/... ./internal/application/ports/...`
- Checkout + storefront shipping rates use shared registry
