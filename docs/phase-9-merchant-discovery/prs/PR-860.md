# PR-860 — Mail Sender Port

Status: done

## Summary

- Add `RegisterMailSender` on `plugin.App` (single winner, mirrors tax)
- `resolveMailer` in `cmd/api/providers.go` with SMTP core default
- Worker email handler uses resolved mailer
- Port introspection updated (`mail_sender` shipped)

## Out Of Scope

- Reference SendGrid-style plugin (PR-861)

## Validation

- `go build ./cmd/api/`
- Port snapshot reports `mail_sender` core default when no plugin registered
