# PR-863 — Reference Checkout Plugin

Status: done

## Summary

- Ship `plugins/checkoutdemo` with positioned validation step `before:create_order`
- Config `plugins.checkoutdemo.enabled` + compile-time registration
- Demonstrates `pluginsdk.CheckoutBefore("create_order")` pattern from PR-854

## Enable

```yaml
plugins:
  checkoutdemo:
    enabled: true
```

## Validation

- `go build ./cmd/api/`
- Plugin registers `checkoutdemo_validate` step; empty cart fails checkout before order creation
