# PR-1024 — OpenTelemetry Traces

Status: planned

## Summary

- Optional OTLP trace export
- Instrument HTTP requests, checkout workflow, and DB/spans behind config

## Why

Metrics without traces still make checkout/payment latency hard to debug. A+ observability bar.

## Scope

- Config: endpoint, enabled flag, sample ratio
- Middleware + checkout step spans; DB if affordable via wrapper
- Default **off**
- Docs for Jaeger/Grafana Cloud collector

## Out Of Scope

- Full baggage/campaign analytics
- Replacing the JSON logger

## Validation

*(planned — run when implementing)*

- `go test ./internal/interfaces/http/... ./internal/application/checkout/... ./internal/platform/... -run 'Trace|OTLP|Telemetry'`
- `go build ./cmd/api/`
- Manual: enabled + local collector shows checkout span

## Documentation updates

| Artifact | Change |
| --- | --- |
| `DEPLOYMENT.md` / `RUNBOOK.md` | tracing setup |
| `config.example.yaml` | otlp keys |

## Follow-up

- PR-1025
