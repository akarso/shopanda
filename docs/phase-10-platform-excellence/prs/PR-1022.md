# PR-1022 — HTTP Package Split (Admin)

Status: planned

## Summary

- Move admin handlers to `internal/interfaces/http/admin`
- Update `cmd/api` wiring only as needed

## Why

Continues PR-1021; isolates privileged surface for review and future auth audits.

## Scope

- Move `*_admin.go` (+ tests) to admin package
- Fix import cycles via shared package
- Behavior-identical

## Out Of Scope

- Storefront move (PR-1023)
- Admin UX features

## Validation

*(planned — run when implementing)*

- `go test ./...`
- `go build ./cmd/api/`
- Spot-check RBAC middleware still wraps admin routes

## Documentation updates

| Artifact | Change |
| --- | --- |
| C4 | admin adapter package |

## Follow-up

- PR-1023
