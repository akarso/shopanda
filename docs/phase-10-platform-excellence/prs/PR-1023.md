# PR-1023 — HTTP Package Split (Storefront)

Status: planned

## Summary

- Move storefront handlers to `internal/interfaces/http/storefront`
- Reduce `StorefrontHandler` dependency surface where cheap (optional constructors split)

## Why

Storefront god-handler (~36 deps, multi-file) is the worst HTTP hotspot after admin split.

## Scope

- Move storefront files + tests
- Optional: split handler into smaller structs (cart/account/catalog) **only if** tests stay green without behavior change
- Prefer mechanical move first; structural split only if low-risk

## Out Of Scope

- Theme engine redesign
- New storefront features

## Validation

*(planned — run when implementing)*

- `go test ./internal/interfaces/http/storefront/...`
- `go test ./...`
- `go build ./cmd/api/`

## Documentation updates

| Artifact | Change |
| --- | --- |
| C4 | storefront adapter package |

## Follow-up

- PR-1024
