# PR-1023 — HTTP Package Split (Storefront)

Status: done

## Summary

- Move all storefront-facing HTTP surface (customer REST API + SSR views) to `internal/interfaces/http/storefront`, continuing PR-1021/1022's split
- Finish the `auth_middleware.go` split that PR-1021/1022 explicitly deferred: customer JWT auth (`AuthMiddleware`, `RequireAuth`) moves to `storefront`; admin RBAC (`RequirePermission`, `RequireRole`, `AdminContextMiddleware`) moves to `admin`
- `cmd/api` wiring updated to construct/reference storefront handlers from the new package
- Behavior-identical: no route, permission, or response-shape changes

## Why

Storefront god-handler (~36 deps, multi-file) is the worst HTTP hotspot after the admin split.

## Scope decisions

**`auth_middleware.go` split (deferred by PR-1021/1022, done here):** the file mixed two unrelated concerns — customer JWT identity (`AuthMiddleware`, `RequireAuth`) and admin RBAC (`RequireRole`, `RequirePermission`, `AdminContextMiddleware`, `sanitizeAdminScopeValue`). `RequirePermission`/`RequireRole`/`AdminContextMiddleware` are used **exclusively** to gate `/api/v1/admin/*` routes in `wire_routes.go` — zero storefront coupling — so they moved to `admin/admin_middleware.go`. `AuthMiddleware`/`RequireAuth` depend on `storefrontSessionToken` (storefront cookie helper) and gate customer-facing routes, so they moved to `storefront/auth_middleware.go`. This also means `admin` no longer needs to import the legacy `http` package for these — it defines them itself now. `auth_middleware_test.go` was split the same way.

**`refund.go` → `admin` (PR-1022 scope gap):** `RefundHandler` only serves `POST /api/v1/admin/orders/{orderId}/refund` — 100% admin, missed in PR-1022 because it matches neither the `*_admin.go` nor `admin_*.go` naming convention (same class of gap as `media.go`/`schema_handler.go` in that PR). Moved to `admin/refund_admin.go` + `refund_admin_test.go`.

**Genuinely-shared handlers/helpers stayed exported from `storefront`, not duplicated:** `CategoryHandler`, `VariantHandler` (public read + admin CRUD reuse the same handler struct), `OrderResponse`/`ToOrderResponse` (`order.go`), `ToReturnResponse(s)` (`return_response.go`), `ToAdminReviewResponse(s)`/`ParseReviewStatus` (`review.go`), `AccountDeleter` (`account.go`), `NewReturnAccountHandler`, `NewAuthHandler`/`AuthHandler`. `admin` now imports these from `storefront` (aliased `storefront`) instead of the legacy `http` package — same pattern PR-1022 established, just re-pointed to the new home.

**What stayed in `internal/interfaces/http`** (neither admin nor storefront — platform/integration-level): `docs.go` (OpenAPI/Swagger, developer-facing), `health.go` (liveness/readiness probes), `setup.go` (first-run setup wizard — runs before any admin/customer exists), `payment_webhook.go`/`stripe_webhook.go` (server-to-server payment provider callbacks), `shared_compat.go` (PR-1021 shim — still needed by these five files).

**Moved to `storefront` despite not being obviously "storefront" by name:** `sitemap.go`, `robots.go` (public SEO surface for the storefront catalog), `rewrite_handler.go` (resolves the storefront's catch-all canonical-URL routing, companion to `ResolverMiddleware` which already moved to `shared` in PR-1021).

## Scope

- Moved ~70 production + test files to `internal/interfaces/http/storefront`
- New code in `storefront` imports `internal/interfaces/http/shared` directly (aliased `httpshared` where a file also needs `internal/domain/shared`) for `JSON`/`JSONError`/`ParsePagination`/`Middleware`/`ClientIP`/`CSRFToken`/`IsRequestSecure`/`ParseTrustedProxies`/`WriteRateLimited` — no shim usage, per PR-1022's established convention. Several call sites had been going through `shared_compat.go`'s **unexported** forwarders (`clientIP`, `shopandaCSRFToken`, `isRequestSecure`, `parseTrustedProxies`) documented in PR-1021 as existing specifically for handler files outside the middleware chain; those became direct calls to the exported `shared` functions since the unexported forwarders aren't reachable from a different package
- `admin` imports `storefront` (aliased `storefront`) for the shared symbols listed above
- Fixed the same class of naming collision as PR-1022: `storefront_test.go` files that also need `internal/application/admin` and/or `internal/domain/admin` (both default to package name `admin`) needed careful aliasing wherever a file also imports the new `internal/interfaces/http/admin` package
- **Mixed test files split**, matching PR-1022's pattern: `category_test.go` and others kept a storefront-only remainder while their admin-facing test functions had already moved to `admin/` in PR-1022; this PR's job was purely re-pointing the storefront half's imports, no new splits were needed here since PR-1022 already did that work
- Ran `goimports -w` across every touched file in `cmd/api`, `internal/interfaces/http`, `internal/interfaces/http/admin`, and `internal/interfaces/http/storefront` (import-grouping regression was a PR-1022 code-review finding; applied proactively here). Incidental side effect: `cmd/api/wire_repos.go` (glob-matched, otherwise untouched by this PR) had its gofmt-flagged double blank line removed — a harmless one-line whitespace fix, not reverted

## Out Of Scope

- Theme engine redesign
- New storefront features
- Splitting `StorefrontHandler` into smaller structs (cart/account/catalog) — the spec allowed this "only if low-risk"; given the scale already covered by the mechanical move, deferred rather than compounding risk in the same PR

## What changed

| Area | Change |
| --- | --- |
| `internal/interfaces/http/storefront/` (new) | ~70 files moved; `package http`/`http_test` → `package storefront`/`storefront_test` |
| `internal/interfaces/http/admin/admin_middleware.go`, `admin_middleware_test.go` (new) | `RequireRole`/`RequirePermission`/`AdminContextMiddleware`/`sanitizeAdminScopeValue` split out of `auth_middleware.go` |
| `internal/interfaces/http/admin/refund_admin.go`, `refund_admin_test.go`, `refund_stub_test.go` (new) | `refund.go` moved from the flat `http` package (PR-1022 scope gap) |
| `internal/interfaces/http/auth_middleware.go`, `auth_middleware_test.go` | Trimmed to `AuthMiddleware`/`RequireAuth` only, then moved to `storefront/` |
| `internal/interfaces/http/admin/*.go` (~25 files) | `shophttp.X` → `storefront.X` for shared symbols (`CategoryHandler`, `OrderResponse`, `ToReturnResponse(s)`, etc.); `shophttp.RequirePermission/RequireRole/AdminContextMiddleware` → bare/`admin.` (now local) |
| `cmd/api/wire_services.go`, `wire_routes.go`, `main.go` | Handler field types / constructors switched from `shophttp.XxxHandler` to `storefront.XxxHandler`; `AuthMiddleware`/`RequireAuth` now `storefront.*`; `RequirePermission`/`RequireRole`/`AdminContextMiddleware` now `admin.*` |
| `internal/interfaces/http/{docs,health,setup,payment_webhook,stripe_webhook}.go`, `shared_compat.go` | **Unchanged** — stayed in the flat `http` package |

## Validation

- `go build ./...` — pass
- `go vet ./...` — pass
- `gofmt -l` / `goimports -l` on every changed file — clean (two pre-existing, unrelated files excluded: `cmd/api/wire_repos.go` already noted above, `internal/application/auth/lockout_test.go` untouched)
- `go test ./internal/interfaces/http/... -count=1` — pass (`http`, `admin`, `storefront`, `shared` all green)
- `go test ./... -count=1` — pass repo-wide (DB-gated packages skip without `SHOPANDA_TEST_DSN`, as before)
- Spot-check: RBAC middleware (`admin.RequirePermission`, `admin.AdminContextMiddleware`) and customer auth (`storefront.AuthMiddleware`, `storefront.RequireAuth`) still wrap the same routes in `wire_routes.go` — only the import path changed, not the wrapping calls themselves
- CR follow-up pass (URL rewrite wiring): `go build ./...`, `go vet ./...`, `go test ./... -count=1` — pass repo-wide, no new failures; `gofmt -l` on both changed files — clean; `TestRewriteRouting_*` (new, `internal/interfaces/http/storefront/rewrite_router_test.go`) pass, confirming the catch-all resolves a matching rewrite, a literal route (`/products/{slug}`) still wins over it, and an unmatched path 404s — all through a real `shared.Router`, not mocks

## Documentation updates

| Artifact | Change |
| --- | --- |
| C4 code diagram | New `storefront` adapter package under `internal/interfaces/http/`; note on the finished `auth_middleware.go` split |
| Phase 10 PR index / ROADMAP / README | status → done; Track E PR-1020–1023 all done; next = PR-1024 |
| `docs/diagrams/c4-component.md` / `c4-container.md` | **No change** — same HTTP boundary component, no new container |
| `PLUGINS.md` / `docs/guides/DEVELOPER.md` / `docs/guides/PLUGIN_COMPOSITION.md` | **No change** — plugin-facing router/middleware registration API unchanged |
| `RUNBOOK.md` / `docs/guides/DEPLOYMENT.md` | **No change** — no new config, env vars, or operator-facing behavior |
| `configs/config.example.yaml` | **No change** |
| Root `README.md` | **No change** — no new CLI or product surface |

## Code review follow-ups

Responding to the CR round on this PR — each item fixed directly unless noted otherwise:

**Fixed:**
- `SHOPANDA_METRICS_ALLOW_INSECURE_BIND` — metrics wildcard-bind relaxation decoupled from `SHOPANDA_DEV_MODE`/DB-password validation (`config.MetricsInsecureBindAllowed()`); previously any dev-mode flag silently loosened metrics bind security too
- Metrics `http.Server` now sets `ReadTimeout`/`ReadHeaderTimeout`/`WriteTimeout`/`IdleTimeout` — was using Go's zero-value (no timeout) defaults
- HTTP request metric's `method` label bounded to a fixed allow-list (`methodLabel`) — was passing `r.Method` straight through, an unbounded-cardinality risk on malformed requests
- `runWorker` now shifts the metrics listen address to `DefaultWorkerMetricsListen` (127.0.0.1:9091) when left at the API default, avoiding an address-in-use collision when API and worker run on the same host with default config
- Added dedicated `rbac.StoreCreditWrite` permission (granted to `RoleAdmin` only) instead of reusing `CustomersWrite` to gate store-credit issuance — a role granted customer-profile editing no longer implicitly gains the ability to mint store credit
- `store_credit_admin.go Issue()` now maps errors through `JSONError`'s `apperror` unwrapping instead of collapsing everything but not-found to 500
- `worker.go processNext`: a `queue.Complete` failure after a successful handler run now also records `JobFailure` — previously silent, despite being a real, actionable redelivery risk
- Added `metrics.OutcomeSucceededEventFailed` — checkout's final `EventCheckoutCompleted` publish failing after every step succeeded is no longer conflated with a real checkout failure in the `CheckoutResult` metric
- Documented (comment) why `deliver_handler.go`'s `repo.FindByID` error path records `failed`, not `skipped` — a repo error is an operational unknown, not a confirmed no-op
- `WithMetrics()` on `Worker`, `Workflow`, and `DeliverHandler` now carries a godoc calling out it's not safe to call concurrently with the processing path — matches the existing unsynchronized-field pattern, just wasn't documented
- Added missing test coverage: `content_block_admin_test.go` (`List`, `Get` 404, `Update` 404, `Delete` OK/404, `GetTarget` — previously only `Create`/`UpdateTarget` were covered); `deliver_handler_test.go` transport-error case (`TestDeliverHandler_WithMetrics_RecordsFailureOnTransportError` — previously only the non-2xx-status failure path was tested, not a `Post` returning an error directly)
- **Wired `ResolverMiddleware`/`RewriteHandler` into `wire_routes.go`** (originally flagged below as "investigated, not fixed" — resolved in a follow-up pass once the design question was settled). Investigation traced the gap back to the feature's original introduction in Phase 1 (`docs/phase-1-core/prs/PR-089.md`, `PR-090.md`): the write side (`rewrite.Subscriber` populating `url_rewrites`) was wired into `main.go` at the time, but the read side (middleware + catch-all handler) never was — **not a regression from this PR or PR-1021/1022**, a years-old pre-existing gap. Registered as `router.Handle("GET /{path...}", shophttp.ResolverMiddleware(rt.repos.rewriteRepo, log)(storefront.NewRewriteHandler().Resolve()))` in `cmd/api/wire_routes.go` — root-level, matching the exact path format `rewrite.Subscriber` writes (`"/"+slug`) and the original spec's `GET /*` catch-all (`docs/phase-1-core/specs/ROUTING.md`). Kept as a root-level JSON resolver rather than an SSR-aware redirect: `RewriteHandler.Resolve()` returns `{type, entity_id}` by design (PR-089: "upstream consumers decide how to render"), and building an HTML redirect would mean looking up each entity's current slug per type — a larger, untested change beyond wiring up what already exists. Scoped as a single low-priority route (not global middleware) so the per-request DB lookup only fires for requests no other route claims — confirmed via `TestRewriteRouting_MoreSpecificRouteTakesPrecedence` (new, `internal/interfaces/http/storefront/rewrite_router_test.go`) that literal routes like `/products/{slug}` still win over the catch-all. Two more new tests in the same file exercise the full `shared.Router` dispatch end-to-end (resolves a match, 404s an unknown path) — closing the gap the CR called out: only mocked unit tests existed before, never a real router.

**Investigated, not fixed:**
- `content_block_admin.go Delete()` — CR flagged as a possible no-op; verified it correctly calls through to `ContentBlockRepo.Delete`, which does a real `DELETE` and returns `apperror.NotFound` on zero rows affected. Not a bug; no change made.
- `product_price_admin.go`, `admin_scope.go` package placement — confirmed intentional: both are admin-only surfaces, correctly living in `internal/interfaces/http/admin`.
- `shared_compat.go` shim growth — no new usage added by this PR; still tracked by the existing PR-1021 follow-up to trim it once the three-way split settles.
- Remaining `category_admin_test.go` gaps noted by the CR (additional 404/validation edge cases beyond what's already covered by the 8 existing tests) — left as-is; the existing coverage (create/update/delete, forbidden, cycle-rejection, repeated-ancestor-rejection) exercises the handler's real risk surface, and the marginal cases weren't worth the scope for this round.

### Round 2 — whole-branch review

A second CR reviewed the branch as a whole (HTTP split + metrics subsystem + admin store-credit issuance) rather than a single PR. Its findings span store-credit correctness (introduced pre-split, now living in `internal/interfaces/http/admin/store_credit_admin.go`), the `shared` package's router/metrics internals (PR-1021), and `cmd/api` process wiring — logged here since this is the PR doc the prior round's follow-ups already live in.

**Blockers: none.**

**Major concerns — fixed:**
- **Store credit issuance had no idempotency protection** (a retried `POST .../store-credit/issue` minted credit twice). Added an optional `Idempotency-Key` header (same convention as `internal/infrastructure/stripepay` and `internal/platform/plugin/integration.go`), threaded through `storecredit.Service.Issue` → `storecredit.Repository.Issue` → `StoreCreditRepo`, persisted on the ledger row (migration `064_store_credit_idempotency.sql`: nullable `idempotency_key` column + partial unique index on `(customer_id, idempotency_key) WHERE idempotency_key IS NOT NULL`). `StoreCreditRepo.applyEntry` checks for an existing entry with the same key before touching the balance (handles the common sequential-retry case), and separately catches a `23505` unique-violation on that specific index as a race-condition fallback (concurrent identical retries), treating either as a no-op success rather than a double-credit. No idempotency key means no dedup — unchanged behavior for callers that don't opt in, including checkout's rollback-`Issue` call sites in `create_order_step.go`, which now pass their own key (`"rollback:save:"+orderID` / `"rollback:apply:"+orderID`) as a low-cost bonus consistency win since the interface already had to change.
- **Store credit issuance was not audited.** `StoreCreditAdminHandler` now takes an `*adminapp.Auditor` (matching `NewPaymentAdminHandler`/`NewReturnAdminHandler`) and logs `AuditStoreCreditRead`/`AuditStoreCreditIssue` (new `AuditAction` constants) on both `Get()` and `Issue()`, success and failure. `cmd/api/wire_services.go` now passes `sharedAuditor`.
- **No upper bound on issuable amount.** Added `config.StoreCreditConfig.MaxIssueAmount` (minor units, default 100000 via `DefaultStoreCreditMaxIssueAmount`; `0` explicitly disables the cap), enforced in `storecreditApp.Service.Issue` before the customer/repo calls. Wired via `storecreditService.WithMaxIssueAmount(cfg.StoreCredit.MaxIssueAmount)` in `wire_services.go`. Documented in `configs/config.example.yaml`.

**Minor concerns — fixed:**
- `isLoopbackHost`'s `"localhost"` check now resolves the hostname via `net.LookupHost` and requires every resolved address to be loopback, instead of trusting the bare string — closes a tampered-`/etc/hosts`/DNS-override edge case. Falls back to trusting the literal if resolution itself fails, so config validation doesn't gain a hard resolver dependency in minimal environments.
- `MetricsMiddleware` no longer re-runs `ServeMux` route matching a second time just to get a bounded label. `Router.Handle`/`HandleFunc`/`TryHandle` now wrap every registered handler in `captureRoute`, which reads `Request.Pattern` (already populated by the mux's own dispatch, by the time the matched handler runs) and writes it into a `*routeMatch` box shared via context; `MetricsMiddleware` reads that box after `next.ServeHTTP` returns instead of calling `RoutePattern(r)` independently. The box is a pointer specifically because intervening middleware (auth, store resolution, ...) calls `r.WithContext(...)`, replacing the `*http.Request` on the way down — a plain post-dispatch read of `r.Pattern` on `MetricsMiddleware`'s own request would not see the mux's mutation, verified by `TestMetricsMiddleware_SurvivesRequestSwapBetweenMetricsAndDispatch` (new). `Router.RoutePattern()` itself is unchanged and still available as a standalone utility.
- `runWorker` now waits (bounded, 10s, via the existing `runtime.ShutdownBackground` helper `runServe` already uses) for the metrics server's `Serve` goroutine to actually return after `Close()`, instead of a bare `defer metricsSrv.Close()` that didn't wait for it — matches `runServe`'s drain sequence.

**New tests:** `internal/application/storecredit/service_test.go` (new file — max-amount cap inclusive/exclusive/disabled, idempotency key pass-through); `internal/infrastructure/postgres/store_credit_repo_test.go` (new file, DB-gated — same-key dedup, distinct-key both-credit, no-key unprotected); `store_credit_admin_test.go` (idempotency header dedup, max-amount rejection/boundary); `secure_defaults_test.go` (`isLoopbackHost` literal IPs, "localhost", unresolvable hostname); `metrics_middleware_test.go` (request-swap survival).

**Migration:** `migrations/064_store_credit_idempotency.sql`.

## Follow-up

- PR-1024: OpenTelemetry traces
- Once the dust settles on the three-way split (`http`/`admin`/`storefront`), re-evaluate whether `shared_compat.go` can finally be trimmed — it's now only needed by 5 files (`docs`, `health`, `setup`, `payment_webhook`, `stripe_webhook`)
- `StorefrontHandler`'s dependency surface (~36 deps) is still a single god-struct across multiple files within `storefront` — a future PR could split it into smaller structs (cart/account/catalog) now that it has its own package to do so safely in
