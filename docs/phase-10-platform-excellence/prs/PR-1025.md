# PR-1025 — pgx Driver Migration

Status: planned

## Summary

- Replace deprecated `lib/pq` with `jackc/pgx/v5` (stdlib `pgx/v5/stdlib` bridge acceptable)
- Keep SQL and repository behavior identical

## Why

`lib/pq` is maintenance-mode; pgx is the maintained driver and improves context/TLS ergonomics.

## Scope

- Swap driver import/registration in `platform/db`
- Fix any pq-specific error type switches → pgx/pgconn equivalents
- CI integration tests green
- Benchmark not required; smoke import/checkout paths

## Out Of Scope

- Rewriting repos to pgx-native APIs
- Connection pool redesign beyond pgx defaults

## Validation

- `go test ./internal/infrastructure/postgres/...` with DSN
- `go test ./...`
- `go build ./cmd/api/`

## Documentation updates

| Artifact | Change |
| --- | --- |
| `go.mod` / `DEPLOYMENT.md` | driver note |
| C4 | infrastructure note if listed |

## Follow-up

- PR-1026
