# PR-1020 — Prometheus Metrics

Status: done

## Summary

- Optional Prometheus `/metrics` (default **disabled**), served on a **dedicated listener** separate from the main app/worker port
- Instrument RED metrics for HTTP plus checkout/job/webhook counters with **bounded labels**

## Why

Zero metrics today. For commerce (orders, payments, queues) logs alone are insufficient for A+.

## Scope

### Exposure / defaults

- Default: metrics **disabled** (`metrics.enabled: false`)
- When enabled: `/metrics` is served on its own `http.Server` bound to `metrics.listen` (default `127.0.0.1:9090`, loopback-only) — **not** mounted on the public API/worker port and **not** behind any app middleware
- No built-in auth on `/metrics` itself; isolation is via bind address (loopback by default) and operator network policy (private scrape network / reverse-proxy auth) if `metrics.listen` is changed to a non-loopback address — documented in RUNBOOK/DEPLOYMENT
- Tests: disabled → recorder is a no-op and `newMetrics` returns a `nil` handler (no listener started); enabled → handler serves Prometheus text format on the configured address only

### Labels (bounded)

- HTTP: route **template** from `http.ServeMux`'s matched pattern (e.g. `/api/v1/products/{id}`) via `Router.RoutePattern`, not raw URL/query string; unmatched routes collapse to a fixed `"unmatched"` label
- Status is bucketed to `2xx/3xx/4xx/5xx` (`statusClass`), not exact codes
- Checkout/webhook outcome is a fixed two-value enum (`success`/`failed`)
- Job failures are labeled by `job.Type`, which is a small fixed set of registered job types (not user input)
- **Excluded** from all labels: raw URLs, query strings, user IDs, order IDs, webhook destination URLs, emails

### Instrumentation

- Client: `prometheus/client_golang` (added as a new dependency)
- `shopanda_http_requests_total{route,method,status_class}` (counter) + `shopanda_http_request_duration_seconds{route,method,status_class}` (histogram) — recorded by `MetricsMiddleware`, the **outermost** middleware in the chain
- `shopanda_checkout_result_total{outcome}` — recorded in `checkout.Workflow.Execute` via `defer`
- `shopanda_job_failures_total{job_type}` — recorded in `jobs.Worker` on handler error or unknown job type
- `shopanda_webhook_deliveries_total{outcome}` — recorded in `webhook.DeliverHandler.Handle` via `defer`, skipping deliveries that were never attempted (e.g. no matching subscription)

## Out Of Scope

- Full Grafana dashboards (example PromQL queries in docs only)
- OpenTelemetry (PR-1024)
- Auth/TLS on the metrics listener (bind-address isolation only; documented as an operator responsibility if exposed beyond loopback)

## What changed

| Area | Change |
| --- | --- |
| `internal/platform/metrics/metrics.go` | `Recorder` port (`HTTPRequest`, `CheckoutResult`, `JobFailure`, `WebhookDelivery`) + `Noop()` |
| `internal/platform/metrics/prometheus.go` | `PrometheusRecorder` (`client_golang` CounterVec/HistogramVec) + `Handler(reg)` |
| `internal/platform/config/config.go`, `validate.go` | `MetricsConfig{Enabled, Listen}`; default `127.0.0.1:9090`; `SHOPANDA_METRICS_ENABLED`/`SHOPANDA_METRICS_LISTEN` env overrides; flatten; normalize defaults `Listen` when `Enabled` and empty |
| `internal/interfaces/http/router.go` | `Router.RoutePattern(req)` exposes the matched `ServeMux` template |
| `internal/interfaces/http/metrics_middleware.go` | `MetricsMiddleware` — RED metrics with bounded route/status-class labels; nil-recorder-safe |
| `internal/domain/jobs/worker.go` | Domain-local `MetricsRecorder` interface (`JobFailure`) + `noopMetrics` default + `WithMetrics` |
| `internal/application/checkout/workflow.go` | `metrics.Recorder` field + `WithMetrics`; `Execute` records success/failed via `defer` |
| `internal/application/webhook/deliver_handler.go` | `metrics.Recorder` field + `WithMetrics`; `Handle` records success/failed via `defer`, skips no-op deliveries |
| `cmd/api/wire_metrics.go` | `newMetrics(cfg)` → recorder + handler (or noop/nil when disabled); `startMetricsServer` runs the dedicated listener with graceful `Close` |
| `cmd/api/main.go`, `wire_services.go`, `wire_routes.go` | Wire recorder through `serveRuntime`/`setupWorker`/checkout/webhook; start metrics server in `runServe`/`runWorker`; `MetricsMiddleware` mounted as outermost middleware |
| Tests | `metrics` package, HTTP middleware, config, jobs/checkout/webhook recorder wiring, `cmd/api` metrics wiring (disabled/enabled/server lifecycle) |

## Code review follow-ups (applied)

| Finding | Action |
| --- | --- |
| Panicking checkout steps recorded as success | Defer `recover`: record `failed`, re-panic |
| Metrics bind failures silent / port collision | Synchronous `net.Listen` in `startMetricsServer`; startup error; docs for distinct ports when colocating serve+worker |
| No enforcement of loopback metrics bind | `validateMetricsListen`: reject wildcard + non-loopback unless `SHOPANDA_DEV_MODE` |
| Malformed webhook job counted as delivery failure | Mark malformed payload as `skipped` (not counted) |
| JobFailure doc overstates handler-not-found case | Clarified comment on `MetricsRecorder` |

## Validation

- `GOTOOLCHAIN=go1.25.13 go test ./internal/interfaces/http/... ./internal/platform/... ./internal/application/checkout/... ./internal/application/webhook/... ./internal/domain/jobs/... ./cmd/api/... -run Metrics -count=1 -race` — pass
- `GOTOOLCHAIN=go1.25.13 go build ./...` — pass
- `GOTOOLCHAIN=go1.25.13 go vet ./...` — pass
- Manual: `metrics.enabled: true` locally, confirm `/metrics` serves Prometheus text on `metrics.listen` only and is absent from the main app port

## Documentation updates

| Artifact | Change |
| --- | --- |
| `RUNBOOK.md` | New "Metrics (Prometheus)" section: disabled by default, dedicated loopback listener, no built-in auth, config keys, exposed metrics + bounded labels, example PromQL |
| `docs/guides/DEPLOYMENT.md` | `SHOPANDA_METRICS_ENABLED` / `SHOPANDA_METRICS_LISTEN` env vars + security note |
| `configs/config.example.yaml` | `metrics.enabled` / `metrics.listen` with inline doc comments |
| `docs/diagrams/c4-component.md` | New `metricsRecorder` component; `Middleware Chain` note "Metrics (outermost)"; `Rel`s from Middleware/CheckoutWorkflow/JobWorker/WebhookDeliverHandler to MetricsRecorder |
| `docs/diagrams/c4-container.md` | `System_Ext(prometheus, ...)`; `apiServer`/`worker` note optional `/metrics` listener; `Rel`s for scraping |
| Phase 10 PR index / ROADMAP / README | status → done; next = PR-1021 |
| `PLUGINS.md` | **No change** — metrics is a platform-level port, not a plugin extension point |
| Root `README.md` | **No change** — no new CLI surface |

## Follow-up

- PR-1021 starts the HTTP package split (Track E continues)
- PR-1024 adds OpenTelemetry tracing behind separate config
