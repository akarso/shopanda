# PR-1003 — CI Integration Job

Status: planned

## Summary

- Second CI job with `postgres:17-alpine` service and `SHOPANDA_TEST_DSN`
- Run postgres package / integration tests that currently skip

## Why

~40 `*_repo_test.go` files only run with a real DSN. Unit CI alone leaves the data layer ungated.

## Scope

- Extend `.github/workflows/ci.yml` with `integration` job
- Service: Postgres 17, create DB/user matching test harness
- Export `SHOPANDA_TEST_DSN`; run `go test ./internal/infrastructure/postgres/...` (and any other DSN-gated packages)
- Job required for merge (same as unit)

## Out Of Scope

- Meilisearch/Redis/RabbitMQ in CI (optional later profiles)
- Load tests

## Validation

- CI green with DSN set
- Confirm at least one previously skipped repo test runs (look for migrate + query in logs)

## Documentation updates

| Artifact | Change |
| --- | --- |
| `.env.example` | Confirm `SHOPANDA_TEST_DSN` docs match CI |
| `docs/guides/DEVELOPER.md` | How to run integration tests locally |

## Follow-up

- Track A complete → unlock Track D/E
