# PR-1012 — Supply-Chain Basics

Status: done

## Summary

- Dependabot for Go modules, GitHub Actions, and Docker base digests
- Pinned `govulncheck` CI job with a documented fail-closed enforcement policy

## Why

Pinned digests/comments say "refresh periodically" with no automation. `lib/pq` and other deps need visibility even before pgx migration.

## Scope

- `.github/dependabot.yml` for `gomod`, `github-actions`, `docker`
- **Fail-closed:** `govulncheck ./...` fails CI on findings not listed in the baseline
- Baseline/exceptions: [`GOVULN_BASELINE.md`](../GOVULN_BASELINE.md) (empty after required upgrades)
- **Pin** `govulncheck` via `GOVULNCHECK_VERSION` in CI
- Document policy + fail-closed verification procedure in `DEVELOPER.md`

Required upgrades to go green (not unrelated drive-bys):

- Go toolchain `1.25.6` → `1.25.13` (stdlib advisories; patch bumps as DB updates)
- `golang.org/x/image` `v0.39.0` → `v0.43.0` (+ transitive `x/text` for that bump)

## Out Of Scope

- Full pgx migration (PR-1025)
- Cosign/SBOM

## What changed

| Area | Change |
| --- | --- |
| `.github/dependabot.yml` | Weekly gomod / actions / docker updates |
| `.github/workflows/ci.yml` | `CI / govuln` job; pinned `GOVULNCHECK_VERSION=v1.7.0` |
| `.github/scripts/run-govulncheck.sh` | Fail-closed runner + baseline/expiry gate |
| `docs/.../GOVULN_BASELINE.md` | Empty baseline + process |
| `go.mod` / `go.sum` | Go 1.25.13; `x/image` v0.43.0 |
| `Dockerfile` | Builder digest bumped to `golang:1.25.13-alpine` (matches `go.mod`; no `GOTOOLCHAIN=auto`) |
| `DEVELOPER.md` | Dependabot + govulncheck + verify procedure |

## Validation

- `GOTOOLCHAIN=go1.25.13 go test ./...` — pass
- `GOTOOLCHAIN=go1.25.13 go build ./cmd/api/` — pass
- `bash .github/scripts/run-govulncheck.sh` (govulncheck@v1.7.0, Go 1.25.13) — pass (0 findings)
- Fail-closed procedure: documented in `DEVELOPER.md` (throwaway branch; do not merge fixture)
- `.github/workflows/ci.yml` / `dependabot.yml` parse as YAML — pass
- Calendar expiry gate: `normalize_ymd` rejects invalid days (e.g. `2026-02-31`) — pass

## Documentation updates

| Artifact | Change |
| --- | --- |
| `DEVELOPER.md` | Dependabot + govulncheck policy + baseline process |
| `RUNBOOK.md` | Short supply-chain pointer |
| `GOVULN_BASELINE.md` | New (empty baseline) |
| Phase 10 PR index / ROADMAP / README | status → done; Track C complete |
| C4 | No change — CI/docs/deps only |

## Follow-up

- Track C complete
- Require `CI / govuln` in branch protection when ready
