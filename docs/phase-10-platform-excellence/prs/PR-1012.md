# PR-1012 — Supply-Chain Basics

Status: planned

## Summary

- Dependabot for Go modules, GitHub Actions, and Docker base digests
- `govulncheck ./...` CI job

## Why

Pinned digests/comments say "refresh periodically" with no automation. `lib/pq` and other deps need visibility even before pgx migration.

## Scope

- `.github/dependabot.yml`
- CI job: install `govulncheck`, run on module; fail on existing vulns or start in report-only with tracked follow-ups (state choice in PR)
- No dependency upgrades bundled unless required to go green

## Out Of Scope

- Full pgx migration (PR-1025)
- Cosign/SBOM

## Validation

- Dependabot PR opens on schedule (or workflow validates config)
- `govulncheck` job runs in CI

## Documentation updates

| Artifact | Change |
| --- | --- |
| `DEVELOPER.md` | mention Dependabot + vuln job |

## Follow-up

- Track C complete
