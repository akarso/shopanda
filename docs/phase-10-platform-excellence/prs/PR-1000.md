# PR-1000 — Unbreak the Test Suite

Status: planned

## Summary

- Restore a green `go test ./...` (without requiring Postgres) for all non-integration packages
- Fix mock drift, assertion drift, and the flaky integration HMAC test

## Why

Three test packages do not compile (`composition`, `importer`, `returns` mocks missing `FindBySKUs` / `SetStocks`). Additional failures: RBAC admin permission count (21 vs 18), extension-port admin status expectation, integrationdemo HMAC timestamp freshness, webhook test mutex-by-value. Without this, CI cannot gate merges.

## Scope

- Update test mocks implementing `catalog.VariantRepository` / `inventory.StockRepository`
- Fix `internal/domain/rbac` test expectations or isolate plugin permission registration between tests
- Fix `TestExtensionPortAdminHandler_ListPorts` expectation (`tax` status)
- Make `plugins/integrationdemo` HMAC test time-stable (inject clock or freeze timestamp)
- Fix `recordingQueue` to hold `*sync.Mutex` / pointer receiver in webhook deliver tests
- Ensure `go vet ./...` and `go test ./...` pass when `SHOPANDA_TEST_DSN` is unset

## Out Of Scope

- Adding GitHub Actions (PR-1002/1003)
- New production features
- gofmt-wide cleanup (PR-1001)

## Validation

- `go vet ./...`
- `go test ./...` (no DSN)
- `go build ./cmd/api/`

## Documentation updates

| Artifact | Change |
| --- | --- |
| Phase 10 PR index | status → done when merged |
| C4 / config / RUNBOOK | No change — test-only |

## Follow-up

- PR-1002 must not merge until this is green
