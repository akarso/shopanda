# PR-1000 — Unbreak the Test Suite

Status: done

## Summary

- Restore a green `go test ./...` (without requiring Postgres) for all non-integration packages
- Fix mock drift, assertion drift, and the flaky integration HMAC test

## Why

Three test packages did not compile (`composition`, `importer`, `returns` mocks missing `FindBySKUs` / `SetStocks`). Additional failures: RBAC admin permission count (21 vs 18), extension-port admin tax status expectation, integrationdemo HMAC timestamp freshness, webhook test mutex-by-value. Without this, CI cannot gate merges.

## Scope

- Updated test mocks implementing `catalog.VariantRepository` / `inventory.StockRepository` in composition, importer, and returns packages
- Fixed `TestPermissionsForRole_Admin` to expect 21 core admin permissions and reset plugin permission pollution
- Fixed `TestExtensionPortAdminHandler_ListPorts` tax status → `core_default`
- Made `plugins/integrationdemo` HMAC test sign with `time.Now()` (inside 5m freshness window)
- Fixed `recordingQueue` Dequeue/Complete/Fail to use pointer receivers (vet: mutex-by-value)

## Out Of Scope

- Adding GitHub Actions (PR-1002/1003)
- New production features
- gofmt-wide cleanup (PR-1001)

## What changed

| Area | Files |
| --- | --- |
| Composition mocks | `listing`/`seo_jsonld` test mocks — `FindBySKUs`, `SetStocks` |
| Importer mocks | `prices_test`, `stock_test`, `products_test` — `FindBySKUs` / `SetStocks` |
| Returns mocks | `service_test.go` — `SetStocks` |
| RBAC | `rbac_test.go` — count + `ResetPluginPermissions` |
| HTTP admin | `extension_port_admin_test.go` — tax status |
| Integration demo | `plugin_test.go` — wall-clock HMAC timestamp |
| Webhook | `deliver_handler_test.go` — pointer methods on `recordingQueue` |

## Validation

- `go vet ./...` — pass
- `go test ./...` (no DSN) — pass (135 packages ok)
- `go build ./cmd/api/` — pass

## Documentation updates

| Artifact | Change |
| --- | --- |
| Phase 10 PR index | status → done |
| Phase 10 ROADMAP | PR-1000 status → done |
| C4 / config / RUNBOOK | No change — test-only |

## Follow-up

- PR-1001 repo hygiene, then PR-1002 CI
