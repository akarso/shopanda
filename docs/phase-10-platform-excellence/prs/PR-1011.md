# PR-1011 — Migration Hygiene CI

Status: done

## Summary

- CI validation for migration filename prefixes with an explicit historical allowlist for the existing `025_*` collision
- Document no-rename policy for applied migrations

## Why

Duplicate `025_*` files exist; tracking is by full filename so it "works" until someone renames. No down migrations — rollback is restore.

## Scope

### Validation policy (fixed before wiring CI)

1. Match files under `migrations/` with `^([0-9]+)_.*\.sql$`
2. Normalize numeric prefixes (strip leading zeros for comparison, or compare as integers — document choice; prefer integer normalization so `025` == `25`)
3. **Allowlist (filename-exact)** the existing collision only:
   - `025_add_cart_merged_guest_id.sql`
   - `025_create_invoices.sql`
4. Fail when:
   - Any **new** file reuses a normalized prefix already present (except the two allowlisted filenames above sharing `25`)
   - An allowlisted historical filename is **renamed** or removed without an explicit policy change PR
5. Wire check into CI (PR-1002 workflow) via Go test under `internal/platform/migrate` **or** a small script invoked by CI
6. Document this exact policy in `DEPLOYMENT.md`

Do **not** rename historical `025_*` in this PR (would break deployed `schema_migrations` rows).

## Out Of Scope

- Introducing golang-migrate/goose
- Writing down migrations for history
- Deleting root `migrations/` files

## What changed

| Area | Change |
| --- | --- |
| `internal/platform/migrate/hygiene.go` | `CheckFilenameHygiene(dir)`: regex-matches `*.sql` filenames, normalizes numeric prefixes as integers, enforces prefix uniqueness except the exact `025_*` allowlist, requires every allowlisted filename to remain present (even if prefix 25 is empty), and fails if an allowlisted prefix's file set doesn't match exactly |
| `internal/platform/migrate/hygiene_test.go` | Real-tree check + synthetic cases including both allowlist entries renamed away / deleted |
| `DEPLOYMENT.md` | New "Migration filename policy" subsection under *Operate PostgreSQL Safely* |

Wired via Go test only — no new CI step. `internal/platform/migrate` is already covered by the existing `CI / unit` job's `go test ./...` (`.github/workflows/ci.yml`), and this check needs no database, so it runs on every PR without touching the integration job.

## Validation

- `go test ./internal/platform/migrate/... -run TestCheckFilenameHygiene -count=1` — pass (includes both-renamed-away / both-deleted allowlist cases)
- `go test ./...` — pass (full suite, no regressions)
- `go vet ./...` — pass
- `gofmt -l internal/platform/migrate/` — clean
- `go build ./cmd/api/` — pass

## Documentation updates

| Artifact | Change |
| --- | --- |
| `DEPLOYMENT.md` | Migration filename policy: regex, integer-normalized uniqueness, exact `025_*` allowlist, no-rename rule, rollback = backup restore |
| Phase 10 PR index / ROADMAP / README | status → done |
| C4 | No change — internal helper in an existing package; no new component/port |

## Follow-up

- PR-1026 expands runbook
