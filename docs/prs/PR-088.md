# PR-088 — Invoice Generation + PDF

## Summary

Adds the **invoice generation** use case: snapshot a paid order into an immutable invoice, render a PDF, store it, and publish the `invoice.created` event.

## Scope

Per ROADMAP Phase 22 — Invoicing, this PR covers:

- Application service for order → invoice generation
- PDF renderer port (domain interface)
- fpdf-based PDF renderer infrastructure implementation
- PDF storage via `media.Storage`
- Event publishing (`invoice.created`)

## Files Changed

### Domain — `internal/domain/invoice/`

| File | Purpose |
|---|---|
| `renderer.go` | `PDFRenderer` port interface — `Render(Invoice) ([]byte, error)` |

### Application — `internal/application/invoice/`

| File | Purpose |
|---|---|
| `service.go` | `Service` with `GenerateFromOrder` — loads paid order, converts items, creates invoice, saves, renders PDF, stores, publishes event |

### Infrastructure — `internal/infrastructure/invoicepdf/`

| File | Purpose |
|---|---|
| `renderer.go` | `Renderer` — implements `PDFRenderer` using `go-pdf/fpdf`; generates A4 PDF with header, metadata, items table, subtotal/tax/total |

### Tests

| File | Tests |
|---|---|
| `internal/application/invoice/service_test.go` | 5 tests — success flow, empty order ID, order not found, order not paid, duplicate invoice |
| `internal/infrastructure/invoicepdf/renderer_test.go` | 3 tests — single item, multiple items, zero tax / USD currency |

**Total: 8 new tests — all passing**

### Dependency

| Module | Version | Purpose |
|---|---|---|
| `github.com/go-pdf/fpdf` | v0.9.0 | Pure-Go PDF generation |

## Design Decisions

1. **PDFRenderer as port**: Defined in the domain layer so the application service depends on an abstraction. The fpdf implementation lives in infrastructure and can be swapped (e.g. for an HTML-to-PDF plugin using headless Chrome).
2. **TaxAmount as input**: Order doesn't carry tax breakdown, so `GenerateInput.TaxAmount` is provided by the caller (tax service, pricing pipeline, or API). This keeps invoice generation decoupled from tax calculation.
3. **Paid-only guard**: Only orders in `paid` status may be invoiced — prevents premature invoice generation.
4. **Idempotency check**: `FindByOrderID` rejects duplicate invoices (backed by the UNIQUE constraint from PR-087).
5. **PDF storage path**: `invoices/<invoice_id>/invoice-<number>.pdf` — deterministic, retrievable via `media.Storage.URL()`.
6. **Event fire-and-forget**: Event publish errors are logged but don't fail the operation (matches existing media service pattern).

## Verification

```bash
go build ./...          # ✅ clean
go vet ./...            # ✅ clean
go test ./...           # ✅ 54 packages pass (8 new tests)
```

## R1 — Review Fixes

All 3 findings verified and fixed:

| # | File | Finding | Fix |
|---|---|---|---|
| 1 | `PR-088.md` | Test counts wrong: "4 tests" for service_test (has 5), total "7" (should be 8) | Updated to "5 tests" and "**Total: 8 new tests**" |
| 2 | `go.mod` | `go-pdf/fpdf` marked `// indirect` but directly imported by `invoicepdf/renderer.go` | Moved to direct `require` block; ran `go mod tidy` |
| 3 | `service_test.go` | Success test only checks `len(storage.saved) == 1` and `PDFPath != ""` — doesn't verify exact path | Built `expectedPath` from invoice ID + number; asserts `result.PDFPath == expectedPath` and `storage.saved[expectedPath]` exists |
