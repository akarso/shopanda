# PR-001: Go module + project skeleton

## Phase
Phase 0 — Project Scaffold

## Scope
`go.mod`, `/cmd/api/main.go`, `/internal` dirs, `.gitignore`

## Files Created
- `go.mod` — `module github.com/akarso/shopanda`, Go 1.25.6
- `cmd/api/main.go` — entry point with `run()` error pattern
- `internal/domain/doc.go` — domain layer marker
- `internal/application/doc.go` — application layer marker
- `internal/infrastructure/doc.go` — infrastructure layer marker
- `internal/interfaces/doc.go` — interfaces layer marker
- `internal/platform/doc.go` — platform layer marker
- `configs/config.yaml` — default config (server, database, log)
- `migrations/.gitkeep` — empty migrations directory
- `.gitignore` — updated with `/bin/`, `shopanda`

## Verification
- `go build ./cmd/api/` — compiles
- `go vet ./...` — passes
- `go run ./cmd/api/` — prints "shopanda starting..." / "shopanda ready"
