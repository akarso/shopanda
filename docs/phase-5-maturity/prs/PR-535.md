# PR-535 — Omnibus Listing Batch Reads (Track D)

Status: done

## Summary

- batch repository methods for variants, current prices, and 30-day lowest history
- `ListingPriceIndicationStep` prefetches once per PLP page instead of per-product N+1

## Why

PR-530 renders Omnibus disclosures on listing cards by calling `ListByProductID`, `FindByVariantCurrencyAndStore`, and `LowestSince` inside a per-product loop. That is correct but scales poorly on large PLPs. Batch reads keep the same lowest-price-variant semantics while reducing query count.

## Scope

- domain ports: `ListByProductIDs`, `FindByVariantsCurrencyAndStore`, `LowestSinceByVariants`
- postgres adapters with store-scoped price fallback on batch price lookup
- `buildListingPriceIndicationsBatch` + refactored `ListingPriceIndicationStep`
- tests: postgres batch queries, composition batch-call count, existing PLP behavior preserved

## Out Of Scope

- PDP `PriceIndicationStep` (single product; no batch need)
- Caching layer or search-index denormalization
- Changing Omnibus business rules or template output

## Validation

- PLP with multiple discounted products still shows correct lowest 30d price per card
- lowest-price variant selection matches PR-530 behavior (same variant as search display)
- disabled Omnibus config still skips all work
- one batch call per repo type per PLP page (not per product)

## Depends On

- PR-530 (done) — listing step and PLP templates

## What changed

- `internal/domain/catalog/variant_repository.go` — `ListByProductIDs`
- `internal/domain/pricing/price_repository.go` — `FindByVariantsCurrencyAndStore`
- `internal/domain/pricing/price_history_repository.go` — `LowestSinceByVariants`
- `internal/infrastructure/postgres/{variant,price,price_history}_repo.go` — batch SQL
- `internal/application/composition/price_indication_builder.go` — `buildListingPriceIndicationsBatch`
- `internal/application/composition/listing_price_indication_step.go` — uses batch builder
- `internal/testutil/repodelegates.go` — test mock batch delegates
